# Build artifacts
/bin/
/dist/
*.exe
*.so
*.dylib
*.dll
*.h

# Editor/IDE
.idea/
.vscode/
*.swp

# Test
coverage.out
/tmp/

# Go workspace — local-only SDK override (see README)
go.work
go.work.sum

# dg local cache
.dg/.cache.json
config.yaml

# Recon working dir (downloaded CLI binaries, raw flows — never committed)
/_work/
